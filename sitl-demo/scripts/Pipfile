[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pymavlink = ">=2.4.37"
pyserial = ">=3.5"
flask = ">=2.3.0"
docker-compose = ">=1.29.0"
psutil = ">=5.9.0"
requests = ">=2.28.0"
numpy = ">=1.21.0"
matplotlib = ">=3.5.0"
pytest = ">=7.0.0"
black = ">=22.0.0"
flake8 = ">=5.0.0"

[dev-packages]

[requires]
python_version = "3.13"
